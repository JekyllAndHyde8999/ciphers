from setuptools import find_packages, setup

setup(
    name="ciphers",
    packages=find_packages(),
    version="0.1",
    description="A library containing implementations of various ciphers. Just import the cipher and call the encode and decode methods.",
    author="Sravan Vinakota"
)
